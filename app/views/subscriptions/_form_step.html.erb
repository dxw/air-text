<% content_for :title, 'Air pollution alert service' %>

<div class="intro mb-2">
  <div class="text-block p-2">
    <h1 class="mb-2">Air pollution alert service subscription</h1>
    <p>Receive alerts by email, text, or voicemail when pollution levels are forecasted to be moderate, high, or very high.</p>
    <p>You can select up to five areas to receive alerts for, and you can unsubscribe from the service at any time.</p>
  </div>

  <aside class="desktop-only mt-3 mr-3">
    <%= render "shared/health_advice" %>
  </aside>
</div>

<hr class="mx-2">

<turbo-frame id="form-frame" data-controller="subscription">
  <div class="form-content p-2">
    <div class="progress-bar my-2">
      <% 4.times do |i| %>
        <span class="<%= i <= @step_group ? "completed" : "" %>"></span>
      <% end %>
    </div>

    <%= form_for @form, url: wizard_path, method: :put, html: { class: "form" }, data: { turbo: @final_step ? false : nil } do |f| %>
      <%= f.hidden_field :current_step, value: @current_step %>

      <% if @form.errors.any? %>
        <div class="form-errors p-2 mb-2">
          <ul>
            <% @form.errors.messages.each do |message| %>
              <li><%= message.second.first %>.</li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= yield f if block_given? %>

      <div class="buttons mt-4 mb-2">
        <% if @first_step %>
          <div></div>
        <% else %>
          <%= link_to "Back", previous_wizard_path, class: "secondary-cta" %>
        <% end %>
        <%= f.submit @final_step ? "Submit" : "Next", class: "primary-cta" %>
      </div>
    <% end %>
  </div>
</turbo-frame>
