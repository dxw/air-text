<%= render 'form_step' do |f| %>
  <div class="text-block">
    <h3>Your email is now verified</h3>
    <p>You can continue the subscription process with this address: <b><%= @form.email %></b></p>
  </div>
<% end %>
